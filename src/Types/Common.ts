interface button {
    children:React.ReactNode,
    className?:string,
}
interface Input {
    type:string,
    placeholder:string,
    className:string
}
interface Search{
    count:number,
    cost:number
}